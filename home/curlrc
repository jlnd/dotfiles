# ~/.curlrc — default curl options.

# When following a redirect, automatically set the previous URL as referer.
referer = ";auto"

# Wait 60 seconds before timing out the connection.
connect-timeout = 60
